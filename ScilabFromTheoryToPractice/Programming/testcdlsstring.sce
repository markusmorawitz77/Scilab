cd SCI      // here SCI is interpreted as a Scilab variable!
ls *.txt    // *.txt is viewed as '*.txt'
cd contrib  // contrib  is interpreted as 'contrib' 
cd ..       // waiting for end of command 
            // equivalent to 'cd home' 
home
