M=mlist(['point','x','y'],0,1)  // creates the point M
typeof(M)                       // M belongs to the point type
M.x                             // coordinate x
M.y                             // coordinate y
